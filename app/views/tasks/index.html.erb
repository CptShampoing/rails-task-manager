<h1>Coucou je suis l'index (pas le doigt)</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %> - <%= task.completed %> <%= link_to 'Destroy', task_path(task.id), method: :delete, data: { confirm: "Sure ?"} %></li>
  <% end %>
</ul>

<%= link_to 'Create a new task', new_path %>
